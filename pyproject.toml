[project]
name = "patch-meta"
version = "0.1.0"
description = "Utility for managing metadata in patches"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests",
]

[project.scripts]
patch-meta = "patch_meta.cli:main"
